# v2.6.1

- Add configurable refusal-keyword detection for video, image post, animated media, and comment image understanding.
- Retry the next visual Provider when a response is empty, raises an error, or matches a refusal keyword.
- Include defaults for common Chinese and English refusal responses and Kiro identity responses, including `I can't discuss that.`.
- Preserve explicit empty keyword lists so refusal detection can be disabled when required.
- Add regression tests for empty, failed, refused, and successful visual Provider responses.