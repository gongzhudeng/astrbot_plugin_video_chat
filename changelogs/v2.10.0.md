# v2.10.0

- Restore Douyin video parsing after the share page changed its nested work payload structure.
- Support snake_case and camelCase media fields while preserving image-post priority.
- Retry yt-dlp once without the configured cookie file when Douyin reports stale cookies, so an expired export does not prevent an otherwise available fallback.