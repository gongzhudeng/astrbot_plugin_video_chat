# v2.5.0

- Add an opt-in image preprocessing switch for static visual inputs and locally extracted video frames.
- Apply configurable longest-edge scaling and JPEG quality at the FFmpeg extraction stage to avoid redundant frame encoding.
- Keep the previous FFmpeg frame extraction parameters unchanged while preprocessing is disabled.
- Preserve original media, avoid upscaling, keep untouched image MIME types, and add focused unit tests.